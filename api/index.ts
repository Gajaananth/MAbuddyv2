import app from './src/index.js';

/**
 * Zium Nova Entry Layer v1.8.0 (TOTAL UNIFICATION)
 * 🦅🛰️ This file is the primary gateway for the Zium Nova engine.
 * The entire backend has been flattened into the api/ folder 
 * to ensure 100% ESM runtime compatibility on Vercel.
 */
export default app;
