{
    "version": 2,
    "buildCommand": "cd client && npm install && npm run build",
    "outputDirectory": "client/dist",
    "rewrites": [
        {
            "source": "/api/cron",
            "destination": "/api/cron.ts"
        },
        {
            "source": "/api/(.*)",
            "destination": "/api/v2_gateway.ts"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}